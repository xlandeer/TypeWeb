{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "${workspaceFolder}/node_modules/.bin/tsc",
            "type": "shell",
            "args": ["-p","${workspaceFolder}/tsconfig.json"],
            "problemMatcher": [
                "$tsc"
            ],
            "presentation": {
                "reveal": "always"
            },
            "group": "build"
        }
    ]
}